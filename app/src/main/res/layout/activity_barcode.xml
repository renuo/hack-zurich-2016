<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="ch.renuo.hackzurich2016.activities.BarcodeActivity">

    <ImageView
        android:id="@+id/main_backgroundImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true"
        android:contentDescription="Risen and Shine"
        android:scaleType="centerCrop"
        android:src="@drawable/rise_and_shine_light" />

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:paddingBottom="180px"
        android:paddingLeft="100px"
        android:paddingRight="100px"
        android:paddingTop="100px"
        android:src="@drawable/add" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:gravity="bottom"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">

        <Button
            android:id="@+id/button2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:backgroundTint="@color/colorPrimary"
            android:onClick="backButtonClicked"
            android:text="Back" />

    </LinearLayout>

    <TextView
        android:id="@+id/textView"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="#ccffffff"
        android:gravity="center"
        android:padding="10dp"
        android:text="Let a family member scan this code by using the 'Join Existing Household' option. If they do not see this option, they should press the menu 'Leave Household'."
        android:textAppearance="?android:attr/textAppearanceMedium" />
</RelativeLayout>
